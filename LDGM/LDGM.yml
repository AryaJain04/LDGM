name: differential_network_env  # Name of the conda environment

channels:  # Channels to search for packages
  - defaults
  - conda-forge

dependencies:  # List of packages to install
  - python=3.8
  - numpy=1.21.0
  - pandas=1.3.3
  - scipy=1.7.1
  - networkx=2.6.3
  - matplotlib=3.4.3
.
